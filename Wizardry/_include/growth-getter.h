#pragma once

#include "gbafe.h"

int HpGrowthGetter(struct Unit*);
int PowGrowthGetter(struct Unit*);
int MagGrowthGetter(struct Unit*);
int SklGrowthGetter(struct Unit*);
int SpdGrowthGetter(struct Unit*);
int LckGrowthGetter(struct Unit*);
int DefGrowthGetter(struct Unit*);
int ResGrowthGetter(struct Unit*);