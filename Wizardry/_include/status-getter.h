#pragma once
#include "gba/types.h"

s8 HpMaxGetter(struct Unit*);
s8 HpCurwGetter(struct Unit*);
s8 PowGetter(struct Unit*);
s8 MagGetter(struct Unit*);
s8 SklGetter(struct Unit*);
s8 SpdGetter(struct Unit*);
s8 LckGetter(struct Unit*);
s8 DefGetter(struct Unit*);
s8 ResGetter(struct Unit*);
s8 ConGetter(struct Unit*);
s8 MovGetter(struct Unit*);
s8 AidGetter(struct Unit*);

